import { useParams } from "react-router-dom";

const Product_Details = () => {
    const id = useParams();
    console.log(id);
    return (
        <div>
            
        </div>
    );
};

export default Product_Details;